{
  "name": "tambola",
  "version": "0.0.1",
  "description": "Tambola Game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "start": "node app.js",
    "dev-start": "cd frontend && npm install && npm run build && cd ../ && nodemon app.js",
    "build-start": "cd frontend && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Winplaybox/tambola.git"
  },
  "author": "Winplaybox",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Winplaybox/tambola/issues"
  },
  "dependencies": {
    "express": "^4.17.1",
    "http": "0.0.1-security",
    "socket.io": "^4.4.1"
  },
  "devDependencies": {
    "@types/socket.io-client": "^1.4.32",
    "nodemon": "^2.0.3",
    "prettier": "^2.0.5"
  },
  "engines": {
    "node": "12.x"
  }
}
